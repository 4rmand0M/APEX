window.__APEX_CONFIG = Object.freeze({
  /* ── Supabase ── */
  SUPABASE_URL: '${SUPABASE_URL}',
  SUPABASE_ANON: '${SUPABASE_ANON}',

  /* ── Gemini (proveedor primario IA) ── */
  GEMINI_API_KEY: '${GEMINI_API_KEY}',

  /* ── Groq (fallback IA) ── */
  GROQ_API_KEY: '${GROQ_API_KEY}',
});
